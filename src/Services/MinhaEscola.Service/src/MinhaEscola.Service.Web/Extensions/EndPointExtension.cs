
namespace MinhaEscola.Service.Web.Extensions
{
    //public static class EndPointExtension
    //{
    //    /// <summary>
    //    /// Mapeamento de rotas padrão
    //    /// </summary>
    //    /// <param name="application"></param>
    //    public static void ExportEndPoints(this WebApplication application) {
    //        var endpoints = Assembly.GetExecutingAssembly().ExportedTypes.Where(x => x.GetInterface(typeof(IEndpointDefinition).Name) != null).Select(x => x);

    //        foreach (Type endpointsType in endpoints) {
    //            IEndpointDefinition? instance = (IEndpointDefinition?)Activator.CreateInstance(endpointsType);
    //            instance?.DefineEndpoints(application);
    //        }
    //    }
    //}
}
